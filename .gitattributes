# Normalize line endings to LF in the repository; let git decide text vs binary.
* text=auto eol=lf

# Shell scripts must keep LF even on a Windows checkout, or the shebang line breaks.
*.sh text eol=lf
